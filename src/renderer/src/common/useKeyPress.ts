

export const useKeyPress = (keys:string[]) =>{
    const onKeys = []
    document.addEventListener('keydown',(e:KeyboardEvent)=>{

    })
    document.addEventListener('keyup',(e:KeyboardEvent) => {
        
    })
}